from setuptools import setup, find_packages

setup(
    name='cartogrammable',
    version='0.1',
    description='Python library to allow the creation of cartograms',
    author='Luke Hales',
    packages=find_packages(),
)