from setuptools import setup, find_packages

setup(
    name='simplegeo',
    version='0.1',
    description='Simple Python library for calculating the area and perimeter of shapes',
    author='Luke Hales',
    packages=find_packages(),
)